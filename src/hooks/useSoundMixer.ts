// import { Sound } from "@/types/domain/types";
// import { LatLng } from "leaflet";
// import { useMemo } from "react";
// import { Howl } from "howler";

// type Props = {
//   sounds: Sound[];
//   mousePosition: LatLng;
// };

// export function useSoundMixer(props: Props) {
//   const howls = useMemo(
//     () => props.sounds.map((sound) => new Howl({ src: sound.audioFile })),
//     [props.sounds]
//   );
// }
